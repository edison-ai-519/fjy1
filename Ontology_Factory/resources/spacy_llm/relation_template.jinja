The text below contains pre-extracted entities, denoted in this format:
<entity text>[ENT<entity id>:<entity label>]

Your task is to extract relations only between those pre-extracted entities.

You must follow every rule below:
1. Output relation labels must be chosen only from this exact label set.
2. Do not output aliases, synonyms, translations, lowercase variants, or any label not listed below.
3. Do not create new entities and do not refer to entities outside the annotated entity ids.
4. If no valid relation exists, return no lines.
5. Every output line must be a valid JSON object using this format:
{"dep": <entity id>, "dest": <entity id>, "relation": <relation label>}

Allowed relation labels:
{%- for label in labels -%}
{{ label }}
{%- endfor -%}

{%- if label_definitions -%}
Below are the allowed labels and their definitions.
Any aliases mentioned in the definitions are for explanation only and are not valid output labels.
{%- for label, definition in label_definitions.items() -%}
{{ label }}: {{ definition }}
{%- endfor -%}
{%- endif -%}

{%- if prompt_examples -%}
Below are some examples (only use them as guidance):
{%- for example in prompt_examples -%}
Text:
'''
{{ preannotate(example) }}
'''
{%- for item in example.relations -%}
{{ item.model_dump_json() }}
{%- endfor -%}
{%- endfor -%}
{%- endif -%}

Here is the text that needs labeling:
Text:
'''
{{ text }}
'''
